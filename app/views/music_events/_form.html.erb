<%= form_with(model: music_event, local: true) do |form| %>
  <% if music_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(music_event.errors.count, "error") %> prohibited this music_event from being saved:</h2>

      <ul>
        <% music_event.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
